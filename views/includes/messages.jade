if messages
  ul
  if messages.success
    each message in messages.success
      li= message
  if messages.warning
    each message in messages.warning
      li= message
  if messages.error
    each message in messages.error
      li= message